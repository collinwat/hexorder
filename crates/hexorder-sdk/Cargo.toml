[package]
name = "hexorder-sdk"
version = "0.16.0"
edition = "2024"
license = "LicenseRef-Proprietary"
publish = false

[dependencies]
bevy = { workspace = true, default-features = false, features = [
  "bevy_state",
  "bevy_log",
] }

[lints]
workspace = true
